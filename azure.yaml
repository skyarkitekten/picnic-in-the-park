# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: picnic-in-the-park

metadata:
  template: azd-init

infra:
  provider: bicep
  path: ./infra

services:
  app:
    project: ./src/app-host/PicnicPlanner.AppHost
    host: containerapp
    language: dotnet
